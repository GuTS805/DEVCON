# Browser-automation scratch output (snapshots, screenshots, console logs).
# Regenerated on demand — nothing here is source.
.playwright-mcp/

# Ad-hoc screenshots saved straight to the repo root during debugging — this
# project has no source images that belong at the top level.
/*.png
/*.jpg
/*.jpeg

# dependencies / build output
node_modules/
.next/
out/

# secrets
.env
.env.*
!.env.example

# editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
